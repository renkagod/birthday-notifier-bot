# Birthday Notifier Bot 🎂

Телеграм бот для уведомлений о днях рождения друзей.

## Возможности
- Уведомления за **неделю, 3 дня, 1 день, 30 минут и 5 минут** до события.
- Простое управление списком через команды.
- Zero-config запуск через Docker.

## Команды
- `/start` - Инструкция
- `/add [Имя] [ДД.ММ.ГГГГ]` - Добавить друга (например: `/add Иван 25.05.1990`)
- `/list` - Показать всех друзей
- `/delete [Имя]` - Удалить друга

## Быстрый старт (Zero Config)

1. Создайте файл `.env` на основе `.env.example` и вставьте туда токен вашего бота:
   ```bash
   cp .env.example .env
   ```
2. Запустите через Docker Compose:
   ```bash
   docker-compose up -d --build
   ```

## Разработка без Docker
1. Установите зависимости: `pip install -r requirements.txt`
2. Создайте `.env`
3. Запустите: `python -m bot.main`
